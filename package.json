{
  "name": "markserv-contrib-mod.dir",
  "version": "1.0.3",
  "description": "Serve a directory listing",
  "repository": "git@github.com:f1lt3r/markserv-contrib-mod.dir",
  "main": "lib/mod.dir.js",
  "template": "lib/mod.dir.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "f1lt3r",
  "license": "GPL-3.0",
  "xo": {
    "esnext": true,
    "space": 2,
    "globals": [
      "it",
      "describe",
      "beforeEach"
    ],
    "rules": {
      "import/no-dynamic-require": 0
    },
    "overrides": [
      {
        "files": "**/Markconf.*",
        "rules": {
          "unicorn/filename-case": [
            "error",
            {
              "case": "pascalCase"
            }
          ]
        }
      }
    ]
  },
  "devDependencies": {
    "xo": "^0.17.0"
  },
  "dependencies": {
    "handlebars": "^4.0.5"
  }
}
